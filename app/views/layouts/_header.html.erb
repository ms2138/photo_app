<nav class="navbar fixed-top navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <div class="logo">
        <%= link_to root_path, id: "logo", alt: 'Photo', title: 'Photo' do %>
            <i class="bi bi-camera-fill"></i>
            <span>Photos</span>
        <% end %>
    </div>
    <div>
        <ul class="nav navbar-nav navbar-right navbar-action-section">
            <% if user_signed_in? %>
                <li>
                    <%= link_to new_user_post_path(current_user), alt: 'New Post', title: 'New post' do %>
                        <i class="fa fa-plus"></i>
                    <% end %>
                </li>

                <li class="dropdown">
                    <a href="#" data-toggle="dropdown" role="button" data-bs-toggle="dropdown">
                        <i class="fa fa-user"></i>
                    </a>
                    <ul class="dropdown-menu">
                        <li><%= link_to "Users",  users_path, class: "dropdown-item" %></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><%= link_to "Profile", current_user, alt: 'Profile', title: 'Profile', class: "dropdown-item" %></li>
                        <li><hr class="dropdown-divider"></li>
                        <li>
                            <%= link_to "Sign out", destroy_user_session_path, alt: 'Sign out', title: 'Sign out', data: { turbo_method: :delete }, class: "dropdown-item" %>
                        </li>
                    </ul>
                </li>
            <% else %>
                <%= link_to new_user_session_path, alt: 'Sign in', title: 'Sign in' do %> 
                    <span><i class="fa fa-sign-in-alt" aria-hidden="true"></i></span>
                <% end %>
            <% end %>
        </ul> 
    </div>
  </div>
</nav>
